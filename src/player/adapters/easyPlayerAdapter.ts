export interface EasyPlayerAdapterPlan {
  enabled: false;
  reason: string;
}

export const easyPlayerAdapterPlan: EasyPlayerAdapterPlan = {
  enabled: false,
  reason: "EasyPlayer.js 是重点评估播放器，许可证和分发方式确认后接入"
};
